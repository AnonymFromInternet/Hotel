{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the about page</h1>
                <p>This data comes from render function {{index .StringMap "defaultData"}}</p>
            </div>
        </div>
    </div>
{{end}}